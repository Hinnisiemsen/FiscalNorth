services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: fiscalnorth
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: secret
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U myuser -d fiscalnorth']
      interval: 2s
      timeout: 5s
      retries: 10

  rabbitmq:
    image: rabbitmq:3-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: myuser
      RABBITMQ_DEFAULT_PASS: secret
    ports:
      - '5672:5672'

  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    container_name: fiscalnorth-backend
    ports:
      - '8080:8080'
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fiscalnorth
      SPRING_DATASOURCE_USERNAME: myuser
      SPRING_DATASOURCE_PASSWORD: secret
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_DEFER_DATASOURCE_INITIALIZATION: 'true'
      SPRING_SQL_INIT_MODE: always
      # Hibernate import.sql only runs with ddl-auto=create/create-drop; load demo seed via Spring SQL init.
      SPRING_SQL_INIT_SCHEMA_LOCATIONS: classpath:org/springframework/session/jdbc/schema-postgresql.sql
      SPRING_SQL_INIT_DATA_LOCATIONS: classpath:import.sql
      SPRING_DOCKER_COMPOSE_ENABLED: 'false'
      SPRING_H2_CONSOLE_ENABLED: 'false'
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: myuser
      SPRING_RABBITMQ_PASSWORD: secret
      SPRING_SESSION_JDBC_INITIALIZE_SCHEMA: always
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      AI_ENABLED: ${AI_ENABLED:-true}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-local-dev-placeholder}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-local-dev-placeholder}
      APP_AUTH_FRONTEND_URL: http://localhost:3000
      STRIPE_ENABLED: ${STRIPE_ENABLED:-false}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      STRIPE_PRICE_ID_MONTHLY: ${STRIPE_PRICE_ID_MONTHLY:-}
      STRIPE_PRICE_ID_YEARLY: ${STRIPE_PRICE_ID_YEARLY:-}
      STRIPE_TRIAL_DAYS: ${STRIPE_TRIAL_DAYS:-14}
      STRIPE_PAST_DUE_GRACE_DAYS: ${STRIPE_PAST_DUE_GRACE_DAYS:-3}
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_started

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    container_name: fiscalnorth-frontend
    ports:
      - '3000:80'
    depends_on:
      - backend
