# finAPI XS2A client (local Maven artifact)

The `openapi-java-client` JAR is not published to Maven Central. CI installs it from this directory before running Maven.

Generate or update via [finAPI SDK Generator](https://documentation.finapi.io/access/access-java-sdk-usage) when upgrading the Berlin Group API version.
